import { ShowHideColumnDirective } from './show-hide-column.directive';

describe('ShowHideColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideColumnDirective();
    expect(directive).toBeTruthy();
  });
});
